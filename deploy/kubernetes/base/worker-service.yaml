apiVersion: v1
kind: Service
metadata:
  name: leadrecovery-worker
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/component: worker
  ports:
    - name: health
      port: 8080
      targetPort: health
      protocol: TCP
