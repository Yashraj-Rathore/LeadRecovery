apiVersion: v1
kind: Service
metadata:
  name: leadrecovery-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
